# User app

Next.js app on port 3000. P2P video, Socket.IO matchmaking, PostHog analytics.

```bash
cp .env.example .env.local
pnpm install
pnpm dev
```

Env: `NEXT_PUBLIC_BACKEND_URL`, `NEXT_PUBLIC_API_KEY`, `NEXT_PUBLIC_POSTHOG_KEY`, `NEXT_PUBLIC_POSTHOG_HOST`.
