# Admin app

Next.js dashboard on port 3001. Better Auth session cookie + TOTP. Chat monitor and kick; no media tap.

```bash
cp .env.example .env.local
pnpm install
pnpm dev
```

Env: `NEXT_PUBLIC_BACKEND_URL`, `NEXT_PUBLIC_TURNSTILE_SITE_KEY`.
