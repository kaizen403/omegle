apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: coturn-ingress
  namespace: turn
  annotations:
    kubernetes.io/ingress.class: "gce"
spec:
  defaultBackend:
    service:
      name: coturn
      port:
        number: 3478
